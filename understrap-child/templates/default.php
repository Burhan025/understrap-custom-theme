<?php
/**
 * Template Name: Default Template (DK)
 *
 * Template for displaying a default page.
 *
 * @package Understrap
 */

// Exit if accessed directly.
defined( 'ABSPATH' ) || exit;

require_once get_stylesheet_directory(  ) . '/inc/site/page.php';
